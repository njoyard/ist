div.array
	@each array
		"{{ this }}"

div.subarray
	@each sub.array
		"{{ property }}"

div.empty
	@each empty
		"will not appear"
		
div.loopindex
	@each array
		"{{ loop.index }}"
		
div.loopouter
	@each array
		"{{ loop.outer.variable }}"
