
	if (isAMD || isNode) {
		define("ist", [], function() { return main; });
	} else {
		var previous = global.ist;

		global.ist = main;
		global.ist.noConflict = function() {
			var ist = global.ist;
			global.ist = previous;
			return ist;
		};
	}
}(this));
