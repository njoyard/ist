{% assign opencurly = '{{' %}
{% assign closecurly = '}}' %}
/* IST template example */
header
    @include "header.ist"
section#main
    @each articles
        article
            h1
                "{{ opencurly }} title {{ closecurly }}"
            "{{ opencurly }} text {{ closecurly }}"
footer
    @include "footer.ist"
